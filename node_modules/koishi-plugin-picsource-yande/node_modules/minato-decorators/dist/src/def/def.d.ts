import { Field } from '@minatojs/core';
declare type Shorthand<S extends string> = S | `${S}(${any})`;
export declare type ModelFieldDef<T = any> = Field<T> | Shorthand<Field.Type<T>>;
export declare type ModelClassType<T = any> = {
    new (...args: any[]): T;
};
export interface PrimaryKeyDef {
    autoIncrement: boolean;
}
export {};
