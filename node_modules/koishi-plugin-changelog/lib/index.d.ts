import { Context, Schema } from 'koishi';
import { DataService } from '@koishijs/plugin-console';
import { components } from '@octokit/openapi-types';
declare module '@koishijs/plugin-console' {
    namespace Console {
        interface Services {
            releases: ReleaseProvider;
        }
    }
}
declare type Release = components['schemas']['release'];
export interface Config {
}
export default class ReleaseProvider extends DataService<Release[]> {
    static using: readonly ["console"];
    static schema: Schema<Config>;
    cache: Promise<Release[]>;
    constructor(ctx: Context);
    get(forced?: boolean): Promise<any>;
}
export {};
