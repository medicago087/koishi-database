import { FunctionParams, FunctionReturn } from '../def';
export declare class RegisterMeta<F extends (...args: any[]) => any, I = any> {
    action: F;
    args: any[];
    info?: I;
    constructor(action: F, args: any[], info?: I);
    run(view: any, ...extras: FunctionParams<F>): FunctionReturn<F>;
}
