import { ClassType } from 'schemastery-gen';
import { Context } from 'cordis';
import { PluginRegistrar } from './plugin-def';
import PluginClass = PluginRegistrar.PluginClass;
import { PartialDeep } from './def';
export declare function CreatePluginFactory<Ctx extends Context, C, IC, P extends {
    config: IC;
}>(basePlugin: PluginClass<Ctx, C, P>, baseConfig: ClassType<IC>): <S>(specificConfig?: ClassType<S>) => new (ctx: Context, config: PartialDeep<S> & C) => P & {
    config: IC & S;
};
