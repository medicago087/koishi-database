import { Dict } from 'cosmokit';
import { Query, Selection } from '@minatojs/core';
export declare class Transformer {
    virtualKey?: string | undefined;
    private counter;
    constructor(virtualKey?: string | undefined);
    createKey(): string;
    protected getActualKey(key: string): string;
    private transformEvalExpr;
    private transformAggr;
    eval(expr: any, group?: Dict): any;
    query(query: Query.Expr): Partial<any> | undefined;
    modifier(stages: any[], sel: Selection.Immutable): void;
}
