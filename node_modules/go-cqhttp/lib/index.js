"use strict";
const path_1 = require("path");
const child_process_1 = require("child_process");
function gocq(options = {}) {
    const args = [];
    if (options.faststart)
        args.push('-faststart');
    return (0, child_process_1.spawn)(gocq.binary, args, options);
}
(function (gocq) {
    gocq.version = 'v1.0.0-rc3';
    gocq.basename = 'go-cqhttp' + (process.platform === 'win32' ? '.exe' : '');
    gocq.binary = (0, path_1.resolve)(__dirname, '../bin', gocq.basename);
})(gocq || (gocq = {}));
module.exports = gocq;
