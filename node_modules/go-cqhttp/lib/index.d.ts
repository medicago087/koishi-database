/// <reference types="node" />
import { SpawnOptions } from 'child_process';
declare function gocq(options?: gocq.Options): import("child_process").ChildProcess;
declare namespace gocq {
    const version = "v1.0.0-rc3";
    interface Options extends SpawnOptions {
        faststart?: boolean;
    }
    const basename: string;
    const binary: string;
}
export = gocq;
