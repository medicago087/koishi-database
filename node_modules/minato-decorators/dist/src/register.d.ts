import { Database, Flatten, Keys } from '@minatojs/core';
import { ModelClassType } from './def';
export declare class ModelRegistrar<Tables> {
    private readonly model;
    constructor(model: Database<Tables>);
    registerModel(cls: ModelClassType<Flatten<Tables[Keys<Tables>]>>): any;
    mixinModel<K extends Keys<Tables>>(tableName: K, classDict: {
        [F in Keys<Tables[K]>]?: ModelClassType<Flatten<Tables[K][F]>>;
    }): void;
}
