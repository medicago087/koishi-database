export declare type Internal<O = any> = {
    [K in keyof O]?: O[K] extends (...args: any) => any ? O[K] : never;
};
declare module '@minatojs/core' {
    interface Model<S> {
        internal: Internal<S>;
    }
}
