import { Schema } from 'koishi';
import Translator from '@koishijs/translator';
declare class BaiduTranslator extends Translator<BaiduTranslator.Config> {
    private _signature;
    translate(options?: Translator.Result): Promise<string>;
}
declare namespace BaiduTranslator {
    interface Config extends Translator.Config {
        appid: string;
        secret: string;
    }
    const Config: Schema<Config>;
}
export default BaiduTranslator;
