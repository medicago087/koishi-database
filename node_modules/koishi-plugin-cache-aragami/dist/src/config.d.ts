import { AragamiOptions, RedisDriverOptions } from 'aragami';
export declare class AragamiRedisConfig implements RedisDriverOptions {
    uri?: string;
}
export declare class AragamiConfig implements AragamiOptions {
    constructor(config: AragamiConfigLike);
    redis?: AragamiRedisConfig;
    getConfig(): AragamiOptions;
}
export declare type AragamiConfigLike = AragamiOptions;
