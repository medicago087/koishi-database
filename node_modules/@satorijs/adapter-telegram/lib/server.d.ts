import { Adapter, Schema } from '@satorijs/satori';
import { TelegramBot } from './bot';
export declare class HttpServer extends Adapter.Server<TelegramBot> {
    start(bot: TelegramBot<TelegramBot.BaseConfig & HttpServer.Config>): Promise<void>;
    stop(): Promise<void>;
}
export declare namespace HttpServer {
    interface Config {
        protocol: 'server';
        path?: string;
        selfUrl?: string;
    }
    const Config: Schema<Config>;
}
