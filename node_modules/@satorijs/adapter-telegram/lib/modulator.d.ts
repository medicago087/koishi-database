import { Modulator, segment } from '@satorijs/satori';
import { TelegramBot } from './bot';
import * as Telegram from './utils';
export declare class TelegramModulator extends Modulator<TelegramBot> {
    private assetType;
    private payload;
    private mode;
    constructor(bot: TelegramBot, channelId: string, guildId?: string);
    addResult(result: Telegram.Message): Promise<void>;
    sendAsset(): Promise<void>;
    flush(): Promise<void>;
    visit(element: segment): Promise<void>;
}
