import { Adapter, Schema } from '@satorijs/satori';
import { TelegramBot } from './bot';
export declare class HttpPolling extends Adapter.Client<TelegramBot> {
    private offset;
    start(bot: TelegramBot<TelegramBot.BaseConfig & HttpPolling.Config>): Promise<void>;
    stop(): Promise<void>;
}
export declare namespace HttpPolling {
    interface Config {
        protocol: 'polling';
        pollingTimeout?: number;
        retryTimes?: number;
        retryInterval?: number;
    }
    const Config: Schema<Config>;
}
