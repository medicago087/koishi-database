import { segment } from '@satorijs/satori';
import * as Telegram from './types';
import { TelegramBot } from './bot';
export declare class Sender {
    private bot;
    private chat_id;
    private errors;
    private results;
    private assetType;
    private payload;
    private mode;
    constructor(bot: TelegramBot, chat_id: string);
    sendAsset(): Promise<void>;
    sendBuffer(): Promise<void>;
    render(elements: segment[]): Promise<void>;
    sendMessage(elements: segment[]): Promise<void>;
    send(content: string): Promise<Telegram.Message[]>;
}
