/// <reference types="node" />
import { Adapter, Schema } from '@satorijs/satori';
import { KookBot } from './bot';
import WebSocket from 'ws';
export declare class WsClient extends Adapter.WsClient<KookBot> {
    _sn: number;
    _ping: NodeJS.Timeout;
    _heartbeat: NodeJS.Timeout;
    prepare(bot: KookBot): Promise<WebSocket>;
    heartbeat(bot: KookBot): void;
    accept(bot: KookBot): Promise<void>;
}
export declare namespace WsClient {
    interface Config extends Adapter.WsClient.Config {
        protocol: 'ws';
        token: string;
    }
    const Config: Schema<Config>;
}
