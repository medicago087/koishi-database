import { Schema, segment } from '@satorijs/satori';
import { KookBot } from './bot';
import * as Kook from './types';
export declare class Sender {
    private bot;
    private path;
    private params;
    private additional;
    private results;
    private errors;
    private buffer;
    constructor(bot: KookBot, channelId: string);
    post(type: Kook.Type, content: string): Promise<void>;
    private transformUrl;
    private _sendCard;
    sendBuffer(): Promise<void>;
    render(elements: segment[]): Promise<void>;
    sendMessage(elements: segment[]): Promise<void>;
    send(content: string): Promise<Kook.Message[]>;
}
export declare namespace Sender {
    type HandleMixedContent = 'card' | 'separate' | 'mixed';
    interface Config {
        handleMixedContent?: HandleMixedContent;
    }
    const Config: Schema<Sender.Config>;
}
