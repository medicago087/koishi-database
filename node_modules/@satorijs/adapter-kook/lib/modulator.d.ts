import { Modulator, Schema, segment } from '@satorijs/satori';
import { KookBot } from './bot';
import * as Kook from './types';
export declare class KookModulator extends Modulator<KookBot> {
    private path;
    private params;
    private additional;
    private buffer;
    constructor(bot: KookBot, channelId: string, guildId?: string);
    post(type: Kook.Type, content: string): Promise<void>;
    private transformUrl;
    private _sendCard;
    flush(): Promise<void>;
    visit(element: segment): Promise<void>;
}
export declare namespace KookModulator {
    type HandleMixedContent = 'card' | 'separate' | 'mixed';
    interface Config {
        handleMixedContent?: HandleMixedContent;
    }
    const Config: Schema<KookModulator.Config>;
}
