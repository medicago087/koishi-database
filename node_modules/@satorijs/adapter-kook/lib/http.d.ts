import { Adapter, Context, Schema } from '@satorijs/satori';
import { KookBot } from './bot';
export declare class HttpServer extends Adapter.Server<KookBot<KookBot.BaseConfig & HttpServer.Config>> {
    constructor(ctx: Context, bot: KookBot);
    start(bot: KookBot): Promise<void>;
}
export declare namespace HttpServer {
    interface Config {
        protocol: 'http';
        path?: string;
        token: string;
        verifyToken: string;
    }
    const Config: Schema<Config>;
}
