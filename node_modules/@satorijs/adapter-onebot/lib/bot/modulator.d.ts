import { Dict, Modulator, segment } from '@satorijs/satori';
import { BaseBot } from './base';
import { CQCode } from './cqcode';
declare class State {
    type: 'message' | 'forward' | 'reply';
    author: Dict;
    children: CQCode[];
    constructor(type: 'message' | 'forward' | 'reply');
}
export declare class OneBotModulator extends Modulator<BaseBot> {
    stack: State[];
    children: CQCode[];
    forward(): Promise<void>;
    flush(): Promise<void>;
    private text;
    visit(element: segment): Promise<void>;
}
export {};
