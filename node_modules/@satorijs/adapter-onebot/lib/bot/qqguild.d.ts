import { Bot, Context, Universal } from '@satorijs/satori';
import { BaseBot } from './base';
import { OneBotBot } from '.';
import * as OneBot from '../utils';
export declare namespace QQGuildBot {
    interface Config extends Bot.Config {
        parent: OneBotBot;
        profile: OneBot.GuildServiceProfile;
    }
}
export declare class QQGuildBot extends BaseBot {
    parent: OneBotBot;
    hidden: boolean;
    constructor(ctx: Context, config: QQGuildBot.Config);
    get status(): Bot.Status;
    set status(status: Bot.Status);
    start(): Promise<void>;
    stop(): Promise<void>;
    getChannel(channelId: string, guildId?: string): Promise<Universal.Channel>;
    getChannelList(guildId: string): Promise<Universal.Channel[]>;
    getGuild(guildId: string): Promise<Universal.Guild>;
    getGuildList(): Promise<Universal.Guild[]>;
    getGuildMember(guildId: string, userId: string): Promise<Universal.GuildMember>;
    getGuildMemberList(guildId: string): Promise<Universal.GuildMember[]>;
}
