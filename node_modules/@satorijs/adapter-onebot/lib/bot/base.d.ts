import { Bot, Fragment } from '@satorijs/satori';
import * as OneBot from '../utils';
export declare class BaseBot<T extends Bot.Config = Bot.Config> extends Bot<T> {
    parent?: BaseBot;
    internal: OneBot.Internal;
    sendMessage(channelId: string, fragment: Fragment, guildId?: string): any;
    sendPrivateMessage(userId: string, fragment: Fragment): any;
    getMessage(channelId: string, messageId: string): any;
    deleteMessage(channelId: string, messageId: string): Promise<void>;
    getSelf(): Promise<import("@satorijs/core").Universal.User>;
    getUser(userId: string): Promise<import("@satorijs/core").Universal.User>;
    getFriendList(): Promise<import("@satorijs/core").Universal.User[]>;
    handleFriendRequest(messageId: string, approve: boolean, comment?: string): Promise<void>;
    handleGuildRequest(messageId: string, approve: boolean, comment?: string): Promise<void>;
    handleGuildMemberRequest(messageId: string, approve: boolean, comment?: string): Promise<void>;
    deleteFriend(userId: string): Promise<void>;
    getMessageList(channelId: string, before?: string): Promise<import("@satorijs/core").Universal.Message[]>;
}
