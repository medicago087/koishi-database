import { Adapter, Context, Quester, Schema } from '@satorijs/satori';
import { OneBotBot } from './bot';
export declare class HttpServer extends Adapter.Server<OneBotBot> {
    bots: OneBotBot[];
    fork(ctx: Context, bot: OneBotBot<OneBotBot.Config & HttpServer.Config>): Promise<void>;
    start(bot: OneBotBot<OneBotBot.Config & HttpServer.Config>): Promise<void>;
    stop(): Promise<void>;
}
export declare namespace HttpServer {
    interface Config extends Quester.Config {
        protocol: 'http';
        path?: string;
        secret?: string;
    }
    const Config: Schema<Config>;
}
