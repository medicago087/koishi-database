/// <reference types="node" />
import { Context, Schema } from 'koishi';
import Assets from '@koishijs/assets';
declare class LocalAssets extends Assets<LocalAssets.Config> {
    private _promise;
    private _stats;
    private path;
    private root;
    private selfUrl;
    constructor(ctx: Context, config: LocalAssets.Config);
    init(): Promise<void>;
    write(buffer: Buffer, filename: string): Promise<void>;
    upload(url: string, file: string): Promise<string>;
    stats(): Promise<Assets.Stats>;
}
declare namespace LocalAssets {
    interface Config extends Assets.Config {
        path?: string;
        root?: string;
        secret?: string;
        selfUrl?: string;
    }
    const Config: Schema<Config>;
}
export default LocalAssets;
