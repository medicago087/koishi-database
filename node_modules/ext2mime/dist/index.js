"use strict";
/* IMPORT */
var consts_1 = require("./consts");
/* EXT 2 MIME */
function ext2mime(ext) {
    ext = ext.trim().toLowerCase().replace(/^\.+/, '');
    for (var i = 0, l = consts_1.EXT_SHORT.length; i < l; i++) {
        if (consts_1.EXT_SHORT[i][1].has(ext))
            return consts_1.EXT_SHORT[i][0] + "/" + ext;
    }
    for (var i = 0, l = consts_1.EXT_FULL.length; i < l; i++) {
        if (consts_1.EXT_FULL[i][1][ext])
            return consts_1.EXT_FULL[i][0] + "/" + consts_1.EXT_FULL[i][1][ext];
    }
    return '';
}
/* EXPORT */
module.exports = ext2mime;
module.exports.default = ext2mime;
Object.defineProperty(module.exports, "__esModule", { value: true });
