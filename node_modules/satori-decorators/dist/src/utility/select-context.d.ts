import { Context, MaybeArray } from 'koishi';
declare const selectors: readonly ["user", "guild", "channel", "self", "private", "platform"];
export declare type SelectorType = typeof selectors[number];
export declare type SelectorValue = boolean | MaybeArray<string | number>;
export declare type BaseSelection = {
    [K in SelectorType]?: SelectorValue;
};
export interface Selection extends BaseSelection {
    and?: Selection[];
    or?: Selection[];
    not?: Selection;
}
export declare function selectContext<Ctx extends Context>(root: Ctx, options: Selection): Ctx;
export {};
