import { Context, Schema, Service } from 'koishi';
import type { PyodideInterface } from 'pyodide';
import { Internal } from './internal';
declare module 'koishi' {
    interface Context {
        nonebot: NoneBot;
    }
}
declare class NoneBot extends Service {
    protected ctx: Context;
    protected config: NoneBot.Config;
    python: PyodideInterface;
    internal: Internal;
    private importTask;
    private installed;
    constructor(ctx: Context, config: NoneBot.Config);
    start(): Promise<void>;
    mount(pathModule: string): string;
    install(pathDeps: string): Promise<void>;
    import(pathModule: string, config?: {}): Promise<void>;
    private loadPackage;
    stop(): Promise<void>;
}
declare namespace NoneBot {
    interface Config {
        packagesFolder?: string;
    }
    const Config: Schema<Config>;
}
export default NoneBot;
