import { Context, segment } from 'koishi';
import { BaseMatcher, CommandMatcher, MessageMatcher } from './matcher';
export declare class Internal {
    protected ctx: Context;
    config: any;
    logger: any;
    constructor(ctx: Context);
    h(type: string, attrs?: any, children?: any): segment;
    get_driver(): {
        config: any;
    };
    on_message(): MessageMatcher;
    on_metaevent(): BaseMatcher;
    on_notice(): BaseMatcher;
    on_request(): BaseMatcher;
    on_startswith(text: string): MessageMatcher;
    on_endswith(text: string): MessageMatcher;
    on_fullmatch(text: string): MessageMatcher;
    on_keyword(text: string | string[]): MessageMatcher;
    on_regex(...args: any[]): MessageMatcher;
    on_command(name: string): CommandMatcher;
    on_shell_command(name: string): BaseMatcher;
}
