import { Context, Schema } from 'koishi';
declare module 'koishi' {
    interface Channel {
        forward: string[];
    }
}
export interface Rule {
    source: string;
    target: string;
    selfId: string;
    guildId?: string;
}
export declare const Rule: Schema<Rule>;
export declare const name = "forward";
export interface Config {
    rules: Rule[];
    interval?: number;
}
export declare const Config: Schema<Config | Rule[], Config>;
export declare function apply(ctx: Context, { rules, interval }: Config): void;
