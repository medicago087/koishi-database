import { Bot, Context, Fragment } from 'koishi';
export declare class SandboxBot extends Bot {
    ctx: Context;
    username: string;
    hidden: boolean;
    internal: {};
    constructor(ctx: Context);
    sendMessage(channelId: string, fragment: Fragment, guildId?: string): Promise<string[]>;
    deleteMessage(channelId: string, messageId: string): Promise<void>;
    getGuildMemberList(guildId: string): Promise<{
        nickname: string;
        userId: string;
    }[]>;
}
export declare const words: string[];
