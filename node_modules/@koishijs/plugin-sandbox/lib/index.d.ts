import { Bot, Context, Dict, Fragment, Schema, User } from 'koishi';
import { DataService, SocketHandle } from '@koishijs/plugin-console';
declare module 'koishi' {
    interface User {
        sandbox: string;
    }
    namespace Session {
        interface Payload {
            handle: SocketHandle;
        }
    }
}
declare module '@koishijs/plugin-console' {
    interface SocketHandle {
        sandbox: SandboxBot;
    }
    interface Events {
        'sandbox/message'(this: SocketHandle, user: string, channel: string, content: string): void;
        'sandbox/user'(this: SocketHandle, name: string, data: Partial<User>): void;
    }
    namespace Console {
        interface Services {
            users: UserProvider;
        }
    }
}
declare class SandboxBot extends Bot {
    ctx: Context;
    username: string;
    hidden: boolean;
    constructor(ctx: Context);
    sendMessage(channel: string, content: Fragment): Promise<string[]>;
    getGuildMemberList(guildId: string): Promise<{
        nickname: string;
        userId: string;
    }[]>;
}
export interface Message {
    user: string;
    channel: string;
    content: string;
}
export declare const words: string[];
export declare class UserProvider extends DataService<Dict<User>> {
    static using: readonly ["database"];
    private task;
    constructor(ctx: Context);
    observe(user: User, users: Dict<User.Observed>): void;
    prepare(): Promise<Dict<User.Observed<keyof User>, string>>;
    stop(): void;
    get(): Promise<Dict<User.Observed<keyof User>, string>>;
}
export declare const name = "sandbox";
export declare const using: string[];
export interface Config {
}
export declare const Config: Schema<Config>;
export declare function apply(ctx: Context, config: Config): void;
export {};
