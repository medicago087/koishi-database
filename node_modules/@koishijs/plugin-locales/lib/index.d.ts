import { Context, Dict, I18n, Schema } from 'koishi';
import { DataService } from '@koishijs/plugin-console';
import { debounce } from 'throttle-debounce';
declare module '@koishijs/plugin-console' {
    namespace Console {
        interface Services {
            locales: LocaleProvider;
        }
    }
    interface Events {
        'l10n'(data: Dict<I18n.Store>): void;
    }
}
export declare const name = "locales";
export interface Config {
    root?: string;
}
export declare const Config: Schema<Config>;
export declare function apply(ctx: Context, config: Config): void;
declare class LocaleProvider extends DataService<Dict<I18n.Store>> {
    static using: readonly ["console"];
    update: debounce<() => Promise<void>>;
    constructor(ctx: Context, config: Config);
    get(): Promise<Dict<I18n.Store, string>>;
}
export {};
