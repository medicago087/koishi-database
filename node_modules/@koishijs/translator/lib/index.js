var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/core/src/index.ts
var src_exports = {};
__export(src_exports, {
  default: () => src_default
});
module.exports = __toCommonJS(src_exports);
var import_koishi = require("koishi");

// packages/core/src/locales/zh.yml
var zh_default = { commands: { translate: { description: "文本翻译", options: { source: "源语言 (默认为自动匹配)", target: "目标语言 (默认为中文)" }, messages: { "expect-input": "请输入要翻译的文本。", output: "原文本：{input}\n语言：{source} -> {target}\n翻译结果：{output}" } } } };

// packages/core/src/index.ts
var Translator = class extends import_koishi.Service {
  constructor(ctx, config) {
    super(ctx, "translator", true);
    this.config = config;
    ctx.i18n.define("zh", zh_default);
    ctx.command("translate <text:text>").userFields(["locale"]).channelFields(["locale"]).option("source", "-s <lang>").option("target", "-t <lang>").action(async ({ options, session }, input) => {
      if (!input)
        return session.text(".expect-input");
      const { source, target } = options;
      const result = { input, source, target, detail: true };
      result.output = await this.translate(result);
      return session.text(".output", result);
    });
  }
};
__name(Translator, "Translator");
var src_default = Translator;
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {});
//# sourceMappingURL=index.js.map
