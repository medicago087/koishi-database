/// <reference types="node" />
import { Assets, Context, Schema } from 'koishi';
declare class LocalAssets extends Assets {
    private config;
    private _promise;
    private _stats;
    private path;
    private root;
    private selfUrl;
    constructor(ctx: Context, config: LocalAssets.Config);
    start(): void;
    stop(): void;
    init(): Promise<void>;
    write(buffer: Buffer, filename: string): Promise<void>;
    upload(url: string, file: string): Promise<string>;
    stats(): Promise<Assets.Stats>;
}
declare namespace LocalAssets {
    interface Config {
        path?: string;
        root?: string;
        secret?: string;
        selfUrl?: string;
    }
    const Config: Schema<Config>;
}
export default LocalAssets;
