/// <reference types="koishi/lib" />
import { Context, Session, Awaitable } from 'koishi';
declare type RequestHandler = string | boolean | ((session: Session) => Awaitable<string | boolean | void>);
export interface VerifierConfig {
    onFriendRequest?: number | RequestHandler;
    onGuildMemberRequest?: number | RequestHandler;
    onGuildRequest?: number | RequestHandler;
}
export declare function verifier(ctx: Context, config?: VerifierConfig): void;
export interface HandlerConfig extends VerifierConfig {
}
export default function apply(ctx: Context, config?: HandlerConfig): void;
export {};
