import { Context } from 'koishi';
export interface Rule {
    match: string | RegExp;
    reply: string | ((...capture: string[]) => string);
}
export interface Config {
    rules?: Rule[];
}
export declare const name = "respondent";
export declare function apply(ctx: Context, { rules }: Config): void;
