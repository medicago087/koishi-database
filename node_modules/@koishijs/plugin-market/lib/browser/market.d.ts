import { MarketProvider as BaseMarketProvider } from '../shared';
export default class MarketProvider extends BaseMarketProvider {
    start(): void;
    collect(): Promise<any>;
    get(): Promise<{
        data: {
            [k: string]: import("@koishijs/registry").AnalyzedPackage;
        };
        failed: number;
        total: number;
        progress: number;
    }>;
}
