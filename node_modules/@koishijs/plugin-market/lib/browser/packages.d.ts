import { PackageProvider as BasePackageProvider } from '../shared';
export default class PackageProvider extends BasePackageProvider {
    static using: string[];
    getManifest(name: string): Promise<import("@koishijs/registry").Manifest>;
    get(forced?: boolean): Promise<{
        [k: string]: BasePackageProvider.Data;
    }>;
}
