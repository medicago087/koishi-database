import { DataService } from '@koishijs/plugin-console';
import { Context, Dict } from 'koishi';
export declare class ServiceProvider extends DataService<Dict<string>> {
    constructor(ctx: Context);
    get(): Promise<Dict<string, string>>;
}
